{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "masterclass.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/curso/:slug",
      "handler": "course.findOne",
      "config": {
        "policies": []
      }
    }
  ]
}
